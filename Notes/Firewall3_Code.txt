struct flow_key {
    __u32 src_ip;
    __u32 dst_ip;
    __u16 src_port;
    __u16 dst_port;
    __u8  proto;
};

struct flow_val {
    __u64 last_seen_ns;
    __u64 first_seen_ns;
    __u64 packets;
    __u64 bytes;
    __u8  state;        // optional (TCP state approximation)
};

BPF_MAP_TYPE_HASH
